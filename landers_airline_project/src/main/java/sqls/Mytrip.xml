<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mytrip">

<select id="insertmytrip" parameterType="com.landers.airline.dto.CalendarDto" resultType="com.landers.airline.dto.UserDto">
select c.calendar_id,c.ticket_id, c.title, c.content, c.wdate, t.user_id
		from calendar c
        inner join ticket t 
        on c.ticket_id = t.ticket_id
        where t.user_id = #{t.user_id};
</select>

</mapper>